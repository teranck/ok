apiVersion: v1
kind: Service
metadata:
  name: caul
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "caul"
    port: 8080
  selector:
    app: caul
