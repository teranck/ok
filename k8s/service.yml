apiVersion: v1
kind: Service
metadata:
  name: womo
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "womo"
    port: 8080
  selector:
    app: womo
