apiVersion: v1
kind: Service
metadata:
  name: reco
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "reco"
    port: 8080
  selector:
    app: reco
